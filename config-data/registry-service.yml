server: 
  port: 8085

eureka:
  instance:
    hostname: localhost
  client:
    registerWhitEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/